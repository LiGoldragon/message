#!/usr/bin/env bash
set -euo pipefail

repo="${MESSAGE_REPO:-$PWD}"
cd "$repo"

cargo fmt --all -- --check
cargo test --all-targets --all-features
cargo clippy --all-targets --all-features -- -D warnings
nix flake check
